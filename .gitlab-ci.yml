stages:
  - build

build_app:
  stage: build
  tags:
    - camera_ui
  script:
    - echo "Đang build ứng dụng cho branch $CI_COMMIT_BRANCH"
    - docker build -t camera_ui:1.0.0 .
    - docker compose up -d